#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

/* float*float (I,Q) : module = amplitude; arg = phase */

int main(int argc, char const *argv[]) {
        /* On fixe l'amplitude à 1 et la fréquence à 10 */
        /* Comme dans l'exemple, on prendra t = 0 */
        /* Ce qui fait qu'on a simplement 0 -> (1.0, 0.0) */
        /*                                1 -> (-1.0, 0.0) */
        return 0;
}
